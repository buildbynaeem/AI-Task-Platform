{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./backend\" --filter \"./frontend\" --filter \"./scripts\" --if-present run typecheck",
    "lint": "prettier --check .",
    "test": "echo \"Skipping tests as no test files found\"",
    "dev:apps": "pnpm -r --parallel run dev",
    "dev:worker": "python3 worker/main.py",
    "dev": "MONGODB_URI=mongodb://127.0.0.1:27017 MONGODB_DB_NAME=taskplatform REDIS_URL=redis://127.0.0.1:6379 JWT_SECRET=dev-secret WORKER_PORT=8082 concurrently \"pnpm run dev:apps\" \"pnpm run dev:worker\"",
    "docker:build": "docker compose build"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
